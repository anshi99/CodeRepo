#include <bits/stdc++.h> 
void reverseArray(vector<int> &arr , int m)
{
	// Write your code here.
	int n=arr.size(),a=1;
	for(int i=m+1;i<=((n-1)+(m+1))/2;i++)
	{
		swap(arr[i],arr[n-a]);
		a++;
	}
}
